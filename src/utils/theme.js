import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#05668D',
    },
    secondary: {
      main: '#028090',
    },
    accent: {
      main: '#02C39A'
    },
    error: {
      main: '#c2185b'
    }
  }
})

export default theme